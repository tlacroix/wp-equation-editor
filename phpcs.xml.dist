<?xml version="1.0"?>
<ruleset name="Equation Editor">
    <description>PHP_CodeSniffer rules for Equation Editor plugin.</description>

    <!-- What to scan -->
    <file>./equation-editor.php</file>
    <file>./includes</file>
    <file>./uninstall.php</file>

    <!-- Exclude third-party libraries and build output -->
    <exclude-pattern>./tiny_mce_wiris/*</exclude-pattern>
    <exclude-pattern>./vendor/*</exclude-pattern>
    <exclude-pattern>./tests/*</exclude-pattern>
    <exclude-pattern>./node_modules/*</exclude-pattern>
    <exclude-pattern>./build/*</exclude-pattern>

    <!-- How to scan -->
    <arg value="sp"/>
    <arg name="basepath" value="."/>
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/>

    <!-- Use WordPress-Core standard with minimal exclusions -->
    <rule ref="WordPress-Core">
        <!-- Allow short array syntax (modern PHP) -->
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
        <!-- Line endings handled by git -->
        <exclude name="Generic.Files.LineEndings"/>
        <!-- Allow embedded PHP in Settings class render methods -->
        <exclude name="Squiz.PHP.EmbeddedPhp"/>
        <!-- Relaxed whitespace rules -->
        <exclude name="Generic.WhiteSpace"/>
        <exclude name="WordPress.WhiteSpace"/>
        <exclude name="Universal.WhiteSpace.PrecisionAlignment"/>
        <exclude name="Generic.Formatting.MultipleStatementAlignment"/>
        <!-- Relaxed formatting rules -->
        <exclude name="Generic.ControlStructures.InlineControlStructure"/>
        <exclude name="PEAR.Functions.FunctionCallSignature"/>
        <exclude name="Squiz.ControlStructures"/>
        <exclude name="Squiz.Functions.FunctionDeclarationArgumentSpacing"/>
        <exclude name="NormalizedArrays.Arrays.ArrayBraceSpacing"/>
        <exclude name="PSR2.Files.EndFileNewline"/>
    </rule>

    <!-- Text domain -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="nuagelab-equation-editor"/>
            </property>
        </properties>
    </rule>

    <!-- Minimum supported PHP version -->
    <config name="testVersion" value="8.0-"/>
</ruleset>
